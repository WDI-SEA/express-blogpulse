<h1><%= article.title %> <small>by <%= article.author.getFullName() %></small></h1>

<p>
  <strong><%= moment(article.createdAt).calendar() %></strong>
</p>

<p><%= article.content %></p>

<hr />

<!-- Form box for comment-->
<h4>New Comment:</h4>
<form method="POST" action="/articles/<% article.id %>/comments">
  <div class="form-container">
    <label for="comment">Thoughts?</label>
    <input id="comment" type="'text" class="form-control" name="content" required>
  </div>

  <div class="form-container">
    <label for="userName">Your Name:</label>
    <input id="userName" type="'text" class="form-control" name="name" required>
  </div>

  <button type="submit" class="btn">Comment</button>
</form>


<!-- Comments to show -- do this first -->
<h3>Comments</h3>
<!-- reference for each article.. get article string 
article.comments.forEach(comment => {
  comment.content comment.name
})
use p tags to show it on the page
-->
  <% article.comments.forEach(comment => { %> 
      
      <p><%= comment.content %></p>
      <p><%= comment.name %></p>
  <% }) %>



<hr />

<a href="/">&larr; Back Home</a>

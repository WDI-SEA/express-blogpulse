<h1>Tag: <%= tag.name.toUpperCase(); %> </h1>

    <% posts.forEach( (post) => { %>
        <div class="well">
            <h3><a href="/posts/<%= post.id %>"> <%= post.title%> </a> </h3>
        </div>
    <% }) %>


<a href="/tags">&larr; Back To Tags</a>